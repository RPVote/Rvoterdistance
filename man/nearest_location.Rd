% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nearest_location.R
\name{nearest_location}
\alias{nearest_location}
\title{Find nearest polling locations for each voter}
\usage{
nearest_location(
  voters,
  locations,
  voter_coords = NULL,
  location_coords = NULL,
  k = 1L,
  max_dist = NULL,
  units = c("km", "miles", "meters"),
  append_data = TRUE,
  progress = FALSE
)
}
\arguments{
\item{voters}{A data frame, matrix, or \code{sf} POINT object containing voter
locations. If a data frame or matrix, must contain lat/lon columns
specified by \code{voter_coords}.}

\item{locations}{A data frame, matrix, or \code{sf} POINT object containing
polling/drop box locations. If a data frame or matrix, must contain
lat/lon columns specified by \code{location_coords}.}

\item{voter_coords}{Character vector of length 2: \code{c("lat_col", "lon_col")}
identifying the latitude and longitude columns in \code{voters}. Ignored if
\code{voters} is an \code{sf} object.}

\item{location_coords}{Character vector of length 2:
\code{c("lat_col", "lon_col")} identifying the latitude and longitude columns
in \code{locations}. Ignored if \code{locations} is an \code{sf} object.}

\item{k}{Integer. Number of nearest locations to return per voter.
Default \code{1}.}

\item{max_dist}{Numeric or \code{NULL}. If not \code{NULL}, return all locations
within this distance of each voter. Units controlled by \code{units}.
Overrides \code{k}.}

\item{units}{Character. One of \code{"km"}, \code{"miles"}, or \code{"meters"}.
Default \code{"km"}.}

\item{append_data}{Logical. If \code{TRUE} (default), include voter and matched
location columns in the output. When \code{k = 1}, also appends the matched
location row.}

\item{progress}{Logical. If \code{TRUE}, print progress for large computations.
Default \code{FALSE}.}
}
\value{
A data frame. If \code{k = 1} and \code{max_dist} is \code{NULL}: one row per
voter with distance columns (\code{distance_m}, \code{distance_km},
\code{distance_miles}). If \code{k > 1} or \code{max_dist} is not \code{NULL}: one row per
voter-location pair with a \code{rank} column.
}
\description{
Calculates the distance between each voter and a set of polling/drop box
locations using the Haversine formula. Can return the single nearest
location, the k nearest, or all locations within a distance threshold.
}
\examples{
data(meck_ev)

# Nearest single location for each voter
result <- nearest_location(voter_meck, early_meck,
  voter_coords = c("lat", "long"),
  location_coords = c("lat", "long")
)
head(result)

# 3 nearest locations per voter
result_k3 <- nearest_location(voter_meck, early_meck,
  voter_coords = c("lat", "long"),
  location_coords = c("lat", "long"),
  k = 3
)
head(result_k3)

# All locations within 10 km
result_10km <- nearest_location(voter_meck, early_meck,
  voter_coords = c("lat", "long"),
  location_coords = c("lat", "long"),
  max_dist = 10, units = "km"
)
head(result_10km)
}
