% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist_to_boundary.R
\name{dist_to_boundary}
\alias{dist_to_boundary}
\title{Distance from voters to a geographic boundary}
\usage{
dist_to_boundary(
  voters,
  boundary,
  voter_coords = NULL,
  units = c("km", "miles", "meters"),
  progress = TRUE
)
}
\arguments{
\item{voters}{A data frame, matrix, or \code{sf} POINT object containing
voter locations. See \code{\link[=nearest_location]{nearest_location()}} for details on input
formats.}

\item{boundary}{An \code{sf} or \code{sfc} object with LINESTRING,
MULTILINESTRING, POLYGON, or MULTIPOLYGON geometry. Will be
transformed to WGS-84 (EPSG:4326) if needed.}

\item{voter_coords}{Character vector of length 2 giving the column
names for latitude and longitude in \code{voters} (e.g.,
\code{c("lat", "lon")}). Required if \code{voters} is a data frame; ignored
for \code{sf} objects.}

\item{units}{One of \code{"km"} (default), \code{"miles"}, or \code{"meters"}.}

\item{progress}{Logical; show progress messages? Default \code{TRUE}.}
}
\value{
Numeric vector of distances (one per voter) in the requested
units.
}
\description{
Computes the minimum great-circle distance from each voter to the
nearest point on a boundary line or polygon edge. The boundary can
represent a river, state border, or any other geographic feature
provided as an \code{sf} geometry object.
}
\details{
For polygon inputs the distance is measured to the polygon's
\strong{boundary} (perimeter), not to its interior. A point inside the
polygon returns the positive distance to the nearest edge.

Core computation uses the spherical cross-track distance formula
implemented in C++ for performance, with bounding-box pruning to
skip distant segments.
}
\examples{
\dontrun{
library(sf)
# Create a simple north-south boundary line
border <- st_sf(
  geometry = st_sfc(
    st_linestring(matrix(c(-109.05, 31.33, -109.05, 37.0),
      ncol = 2, byrow = TRUE
    )),
    crs = 4326
  )
)

voters <- data.frame(lat = c(35.08, 32.0), lon = c(-106.65, -108.5))
dist_to_boundary(voters, border, voter_coords = c("lat", "lon"))
}
}
